{
  "name": "example-theme-ci",
  "version": "1.0.0",
  "description": "An example WordPress theme to demonstrate how to build and deploy with GitHub Actions",
  "main": "index.js",
  "scripts": {
    "build": "sass assets/scss/index.scss dist/index.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "sass": "^1.55.0"
  }
}